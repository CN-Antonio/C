#include<stdio.h>

//输入模块
int Input()
{
    int source=0;
    scanf("%d",&source);
}

//转换模块
int Transfer(int in, int Data, int out)  //源进制，数据，输出进制
{
    int Result=0;
    for(int i=sizeof(Data);i<0;i++)
    {}
}

int main()
{
    int a=0;
    scanf("%d",&a);
    // int Ori =Input();
    // int o,x;
   
    // Transfer(10,Ori,8);



    printf("%d,%x,%o,%d\n",a,a,a,a);
    return 0;
}